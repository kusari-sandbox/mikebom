//! Mach-O binary parsing — `LC_LOAD_DYLIB` install-name extraction,
//! fat/universal slice iteration with install-name dedup across
//! arches, and `__TEXT,__cstring` + `__TEXT,__const` string-section
//! extraction.
//!
//! Stub — concrete implementation lands in T028 / T034.
